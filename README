Example docker container monitoring the weather temperature at Clermont-Ferrand (FR) and storing result in InfluxDB database.

Env vars
========

`INFLUXDB_HOST`: InfluxDB hostname or IP

`INFLUXDB_PORT`: InfluxDB port

`INFLUXDB_NAME`: InfluxDB database name

`INFLUXDB_USER`: InfluxDB username

`INFLUXDB_PASSWORD`: InfluxDB password

`FORCE_TEMP`: Force temp to given value (in case of quota exceeded)

`RANDOM_TEMP`: Set to return random value
